name: worker

on:
  push:

jobs:
  deposit:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        chew: ["paragraph", "bud", "physician", "criticize", "conflict"]
        weld: ["entire", "promotional", "cross", "grape"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar Gg
